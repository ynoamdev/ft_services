apiVersion: batch/v1
kind: Job
metadata:
  name: wordpress
spec:
  template:
    metadata:
      name: wordpress-pod
    spec:
      containers:
      - name: wordpress
        image: wordpress
        imagePullPolicy: Never
      restartPolicy: Never
